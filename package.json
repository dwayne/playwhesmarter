{
  "name": "playwhesmarter",
  "version": "0.0.0",
  "private": true,
  "description": "The Play Whe Smarter website.",
  "scripts": {
    "build:js": "babel js -d output",
    "build:css": "node-sass -o output sass"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "node-sass": "^4.7.2"
  }
}
